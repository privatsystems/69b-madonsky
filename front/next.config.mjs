
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'privat.systems', 'back.madansky.com']
  },
};

export default nextConfig;
